!!!
%html
  %head
    %meta{ content: 'text/html; charset=UTF-8', 'http-equiv' => 'Content-Type' }
    %title= [content_for(:page_title), SITE_NAME].compact.join(' - ').html_safe
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'
    %link(rel="shortcut icon" type="image/x-icon" href="/favicon.ico")
    %link(href='http://fonts.googleapis.com/css?family=Lato:700' rel='stylesheet' type='text/css')
    = csrf_meta_tags
  %body
    %main
      %header
        = gravatar_image_tag 'jake@worth-chicago.co'
        %h1= link_to SITE_NAME, root_path
        %ul
          - if developer_signed_in?
            %li.action-button= link_to '+', new_post_path
            %li.action-button= link_to '☮', destroy_session_path
          %li= link_to 'Github', 'http://github.com/jwworth', target: '_blank'
          %li= link_to 'Twitter', 'http://twitter.com/jwworth', target: '_blank'
          %li= link_to 'Email', 'mailto:jake@worth-chicago.co'
        .subheader= "A developer's notebook"
      #flash
        - if flash.present?
          - if flash[:alert]
            - [flash[:alert]].flatten.each do |alert_message|
              %p.alert= alert_message
          - elsif flash[:notice]
            - [flash[:notice]].flatten.each do |notice|
              %p.notice= notice
      %article
        = yield

      %footer
        %p
          &copy;
          #{Date.today.year} Jake Worth
          \//
          = link_to 'Source', 'http://github.com/jwworth/worth-chicago.co', target: '_blank'
