- content_for :page_title, 'About'

.page-header
  %h3 About

.container
  .col-md-6
    %h4 Projects
    .content
      - if projects.any?
        - projects.each do |project|
          %p.title
            = link_to project.title, project.hyperlink
            - if developer_signed_in?
              = link_to '[edit]', edit_project_path(project)
          %p= project.description
      - else
        None.

  .col-md-6
    %h4 Speaking Engagements
    .content
      - if speaking_engagements.any?
        - speaking_engagements.each do |engagement|
          %p.title
            = succeed ',' do
              %em= link_to_if(engagement.hyperlink, engagement.title, engagement.hyperlink)
            = succeed ',' do
              = engagement.location
            = engagement.date.strftime('%B %Y')
      - else
        None.


.container
  .col-md-6
    %h4 Other Writing
    .content
      I sometimes write for the Hashrocket Blog, collected
      = succeed '.' do
        = link_to 'here', 'https://hashrocket.com/blog/rocketeers/jake-worth'
